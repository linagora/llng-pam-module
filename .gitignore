# Build directory
build/
build-core/
build-desktop/
build-test/

# Editor files
*~
*.swp
.vscode/
.claude/
.idea/

# Compiled files
*.o
*.so
*.so.*

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile

# Debug
*.dSYM/

# Debian build artifacts
debian/.debhelper/
debian/tmp/
debian/open-bastion/
debian/open-bastion-desktop/
debian/open-bastion-dbgsym/
debian/open-bastion-builder/
debian/*.debhelper.log
debian/*.substvars
debian/*.postinst.debhelper
debian/*.postrm.debhelper
debian/debhelper-build-stamp
debian/files
obj-x86_64-linux-gnu/

CLAUDE.md
.PLAN.md
.omc/
local/

# Local libvirt lab: VM disk images, seeds and the mkvm helper are
# machine-local scratch, never part of the source archive.
local-test/vm/

# Verification build trees (ad-hoc build-and-test recipes, sanitizers)
build-verify/
build-asan/
bv/

# Deployment bundles produced by ob-builder (#203).
#
# A bundle built with client_secret_mode=embedded carries the OIDC client
# secret in clear text in roles/open-bastion/defaults/main.yml, and its
# inventory names production hosts. Generate bundles OUTSIDE this working
# tree; these entries only cover the ones that are already here.
/linagora-setup-bastion/
/linagora-setup-backend/
/old-linagora-setup/
/*-setup-bastion/
/*-setup-backend/
/*-setup-standalone/

# Presentation material dropped in the tree
/Open-Bastion.zip

# Upstream source packages unpacked for reference (openssh, pam)
/openssh_*.orig.tar.*
/openssh_*.debian.tar.*
/openssh_*.dsc
/pam_*.orig.tar.*
/pam_*.debian.tar.*
/pam_*.dsc
/pam-[0-9]*/
